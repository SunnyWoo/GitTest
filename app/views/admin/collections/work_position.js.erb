$('body').append("<%= j render 'position_form', work: @work, collection: @collection %>");
$("#position_form_<%= @work.id %>").modal('show');
$('#position_form_<%= @work.id %>').on('hidden.bs.modal', function(e) {
  $(e.target).remove();
});
